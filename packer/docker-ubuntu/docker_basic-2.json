{
    "builders": [{
        "type": "docker",
        "image": "ubuntu:16.04",
        "commit": true
    }],
    "provisioners": [{
        "type": "shell",
        "script": "../scripts/docker-install.sh"
    }],
    "post-processors": [
        [
            {
                "type": "docker-tag",
                "repository": "localhost:5000/ubuntu16-04",
                "tag": "0.1"
            },
            "docker-push"
        ]
    ]
}