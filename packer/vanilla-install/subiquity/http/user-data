#cloud-config
autoinstall:
  version: 1
  apt:
    geoip: true
    preserve_sources_list: false
    primary:
    - arches: [amd64]
      uri: http://us.archive.ubuntu.com/ubuntu
  identity:
    hostname: ubuntu2004
    username: vagrant
    password: '$6$pYP9uw5wpvWxMapn$QUwvTLT2rOCSFQqRMkPAb5qIC3toclUNueOkBV./cCBYa5ZKywCgZ5WughkseNIIMDYdd3mHzBwuXNcKsc/aE1'
  ssh:
    allow-pw: true
    install-server: true
  locale: en_US
  keyboard: 
    layout: us
  storage:
    layout:
      name: direct
    config:
      - type: disk
        id: disk0
        match:
          size: largest
      - type: partition
        id: boot-partition
        device: disk0
        size: 2048M
      - type: partition
        id: root-partition
        device: disk0
        size: -1
  late-commands:
    - "echo 'Defaults:vagrant !requiretty' > /target/etc/sudoers.d/vagrant"
    - "echo 'vagrant ALL=(ALL) NOPASSWD: ALL' >> /target/etc/sudoers.d/vagrant"
    - "chmod 440 /target/etc/sudoers.d/vagrant"



